name: Scraper BadNet

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 geopy

      - name: Run scraper
        run: |
          python scraper.py

      - name: Commit & push updated JSON
  run: |
    git config --local user.email "action@github.com"
    git config --local user.name "GitHub Action"
    git add tournois.json
    git commit -m "Mise à jour automatique tournois BadNet" || echo "Pas de changements"
    git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/gauvainmeulle-wq/badnet-km100.git HEAD:main


